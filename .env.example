# eScooterAI - 환경 변수 설정
# 이 파일을 .env로 복사하고 본인의 환경에 맞게 수정하세요.
# cp .env.example .env

# ============================================================================
# 프로젝트 설정
# ============================================================================
# 프로젝트 루트 경로 (절대 경로)
PROJECT_ROOT=/Users/username/workspace/eScooterAI

# ============================================================================
# OpenAI API (RAG 시스템 필수) 🔬
# ============================================================================
# OpenAI API 키 발급: https://platform.openai.com/api-keys
#
# 사용 모델:
#   - text-embedding-3-small: 문서 벡터화 ($0.00002/1K tokens)
#   - gpt-4-turbo-preview: 답변 생성 ($0.01/1K input, $0.03/1K output)
#   - gpt-3.5-turbo: 답변 생성 (저렴, $0.0005/1K input, $0.0015/1K output)
OPENAI_API_KEY=sk-your-openai-api-key-here

# ============================================================================
# RAG 시스템 설정 🔬
# ============================================================================
# 벡터 저장소 경로 (FAISS 인덱스 저장)
VECTOR_DB_PATH=./vector_db

# 임베딩 차원 (OpenAI text-embedding-3-small: 1536)
EMBEDDING_DIMENSION=1536

# Top-K 검색 결과 개수 (5 권장 - 검색 정확도 향상)
# - Top-K=3: 빠른 응답, 일부 법규 문서 누락 가능
# - Top-K=5: 정확한 검색, 응답 시간 약간 증가 (+0.7s)
RAG_TOP_K=5

# LLM 모델 선택 (gpt-4-turbo-preview 권장, gpt-3.5-turbo 저렴)
RAG_LLM_MODEL=gpt-4-turbo-preview

# LLM Temperature (0.0~1.0, 낮을수록 일관적, 높을수록 창의적)
# RAG는 사실 기반 답변이므로 0.3 권장
RAG_TEMPERATURE=0.3

# LLM Max Tokens (답변 최대 길이, 500~1000 권장)
RAG_MAX_TOKENS=500

# ============================================================================
# Telegram Bot 알림 설정
# ============================================================================
# Telegram Bot Token (BotFather에서 발급받은 토큰)
# 발급 방법: Telegram에서 @BotFather 검색 → /newbot 명령어 → Bot 이름 설정
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Telegram Chat ID (알림을 받을 사용자 또는 그룹의 Chat ID)
# 확인 방법: Bot에게 /start 전송 후, 아래 URL 접속
# https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
TELEGRAM_CHAT_ID=your_chat_id_here

# Telegram 알림 활성화 여부 (true 또는 false)
TELEGRAM_ALERTS_ENABLED=false

# ============================================================================
# 모델 설정
# ============================================================================
# YOLOv8 모델 경로 (SafetyVisionAI 사전 훈련 모델)
YOLO_MODEL_PATH=./models/ppe_detection/weights/best.pt

# 신뢰도 임계값 (0.0~1.0, 기본: 0.25)
DETECTION_CONFIDENCE=0.25

# IoU 임계값 (0.0~1.0, 기본: 0.45)
DETECTION_IOU=0.45
