# Lightweight dependencies for Streamlit Cloud & Hugging Face Spaces
# Safety Vision AI - Streamlit Web Interface
# Updated: 2025-11-25 - Added WebRTC support

# CRITICAL: opencv-python-headless must be installed BEFORE ultralytics
# ultralytics depends on opencv-python, but we need the headless version
# for cloud deployment (no GUI dependencies)

# Image processing - Install first (opencv-python-headless replaces opencv-python)
opencv-python-headless==4.10.0.84
pillow>=9.5.0

# Data processing
numpy>=1.24.0,<2.0.0
pandas>=2.1.0

# Core packages
ultralytics==8.3.229
streamlit==1.51.0
plotly==6.5.0

# Real-time webcam detection (WebRTC)
streamlit-webrtc>=0.47.0
av>=10.0.0  # PyAV for video encoding/decoding
aiortc>=1.5.0  # WebRTC implementation

# Voice alert system
gTTS>=2.5.0
pygame>=2.5.0

# Utilities
python-dotenv>=1.2.1
requests>=2.31.0  # For Telegram Bot API
